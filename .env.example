# Local (fallback por defecto si falta)
DATABASE_URL=sqlite:///./dev.db

# Railway interno (para PROD; no usar local):
# postgresql+psycopg://postgres:***@postgres.railway.internal:5432/railway

# Otros (solo placeholders, NO valores reales):
GOOGLE_CALENDAR_TOKEN_JSON=...
TELEGRAM_TOKEN=...
